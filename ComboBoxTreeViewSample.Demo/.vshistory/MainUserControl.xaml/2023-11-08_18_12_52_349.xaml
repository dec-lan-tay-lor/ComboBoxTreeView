<UserControl x:Class="ComboBoxTreeView.Demo.MainUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:tv="clr-namespace:VortexWolf.Controls;assembly=VortexWolf.Controls"
             mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">
    <UserControl.Resources>
        <HierarchicalDataTemplate x:Key="treeViewDataTemplate" ItemsSource="{Binding Children}">
            <TextBlock Text="{Binding Title}" />
        </HierarchicalDataTemplate>
    </UserControl.Resources>
    <StackPanel x:Name="LayoutRoot" Background="White" HorizontalAlignment="Center" VerticalAlignment="Top" Width="200">
        <tv:ComboBoxTreeView x:Name="comboBox" 
                
                                ItemTemplate="{StaticResource treeViewDataTemplate}" />
        <TextBlock x:Name="textBlock" />
    </StackPanel>
</UserControl>
